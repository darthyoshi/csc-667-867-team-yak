
<div class='artwork'>	

<% @artworks.each do |artwork| %>
   <h4><%= artwork.title %></h4>
   <%= link_to image_tag(artwork.imagepath, { class: "img-responsive" }), artwork_path(artwork.id) %>
<br><br>
<% end %>
<%= will_paginate @artworks %>
</div>
