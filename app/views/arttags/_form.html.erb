<%= form_for(@arttag) do |f| %>
  <% if @arttag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@arttag.errors.count, "error") %> prohibited this arttag from being saved:</h2>

      <ul>
      <% @arttag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tagname %><br>
    <%= f.text_field :tagname %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
