<%= form_for(@shoppingcartitem) do |f| %>

  <% if @shoppingcartitem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shoppingcartitem.errors.count, "error") %> prohibited this shoppingcartitem from being saved:</h2>

      <ul>
      <% @shoppingcartitem.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :artwork_id %>
    <%= f.number_field :artwork_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :quantity %>
    <%= f.number_field :quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
